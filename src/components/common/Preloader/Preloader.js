import preloader from "../../../accets/images/preloader.gif";
import React from "react";


let Preloader = (props) => {
    return <div>
        <img src={preloader} />
    </div>
}
export default Preloader;